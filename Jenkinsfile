pipeline {
    agent any
    stages {
        stage('Clone') {
            steps {
                git 'https://github.com/https://github.com/ShobitPandey/https://github.com/ShobitPandey/simple-web-app.git'
            }
        }
        stage('Build') {
            steps {
                echo 'Building...'
                // Add build steps here (if any)
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying...'
                // Add deployment steps here (if any)
            }
        }
    }
}
